# No secrets are required to run this project.
# All study data stays in the browser's IndexedDB.
# Optional: hosted acceptance testing against a deployed URL.
# PLAYWRIGHT_BASE_URL=https://example.pages.dev
